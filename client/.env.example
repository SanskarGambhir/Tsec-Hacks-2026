# Base URL of the API, including the version prefix.
VITE_SERVER_URL=http://localhost:8000/api/v1

# Razorpay publishable key id. Must match RAZORPAY_KEY_ID on the server, or
# checkout will open against the wrong merchant account.
VITE_RAZORPAY_KEY_ID=
